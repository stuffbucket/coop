# Coop dependencies for macOS
# Install with: brew bundle
#
# Required packages are installed/upgraded automatically.
# Run `brew bundle` to install all dependencies.

# VM backend (runs Incus inside a Linux VM) - REQUIRED
# Minimum version: 0.7.0 (Incus runtime support added)
brew "colima"

# Incus client tools (talks to Incus inside VM) - REQUIRED  
brew "incus"

# JSON parsing for scripts - OPTIONAL (uncomment if needed)
# brew "jq"
